const AccountSettings = ({ title, description }: any) => (
  <div className="mt-4">
    <h3 className="text-lg font-semibold">{title}</h3>
    <p className="text-gray-500">{description}</p>
  </div>
);

export default AccountSettings;
